apiVersion: v2
name: spruch-app
description: Helm Chart fuer die Spruch-Applikation (Frontend & Backend API)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: ingress-nginx
    version: "4.11.2"
    repository: "https://kubernetes.github.io/ingress-nginx"
    condition: ingress-nginx.enabled