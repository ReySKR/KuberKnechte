[project]
name = "spruch-frontend"
version = "1.0.0"
description = "Frontend to retrieve quote endpoint"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit>=1.62.0",
]

[dependency-groups]
dev = [
    "python-dotenv>=1.2.3",
]
